import { defineConfig } from 'astro/config';

// Static personal portfolio. Set `site` to your final domain before deploy
// (used for canonical URLs / sitemaps if you add them later).
export default defineConfig({
  site: 'https://jakeboersma.com',
});
